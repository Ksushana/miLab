extends ./base.pug


block body
  .layout( class=(isOverflowHidden ? "overflow" : "" ))
    include ./../partials/header.pug

    main.content(class={'content-not-found': NOT_FOUND})
      block content

    include ./../partials/footer.pug
