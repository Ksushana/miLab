header.header(id="top")
  .header__container.container
    a(href="./").header__logo
      span( class=(isLila ? "lila" : isBlue ? "blue" : isOrange ? "orange" : isGreen ? "green" : isLightGreen ? "light-green" : isPeach ? "peach" : "purple"))
      include ./../../inline-svgs/logo.svg
    .header__burger
      button( type="button" )
        include ./../../inline-svgs/burger.svg
    .header__close.visually-hidden
        button( type="button" )
          include ./../../inline-svgs/cross.svg
    .header__menu
      .header__links
        .header__link.notes.link( class=(isSoftware ? "inactive" : null) )
            a(href="./software") Software
        .header__link.notes.link( class=(isKits ? "inactive" : null) )
            a(href="./kits") Kits
        .header__link.notes.link( class=(isServices ? "inactive" : null) )
            a(href="./services") Services
        .header__link.notes.link( class=(isAbout ? "inactive" : null) )
            a(href="./about") About
        .header__link.notes.link( class=(isPublications ? "inactive" : null) )
          a(href="./publications") Publications
      .header__contact-link.notes.link
        a(href="./contacts") Contact
      .header__mobile-bottom
        p © 2016-2021 All rights reserved
        include ./../../inline-svgs/sk.svg
